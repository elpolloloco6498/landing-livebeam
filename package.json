{
  "name": "landing-livebeam",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "predeploy": "npm run build",
    "build": "vite build",
    "deploy": "touch ./dist/.nojekyll && gh-pages -d dist -t true",
    "preview": "vite preview"
  },
  "devDependencies": {
    "gh-pages": "^6.1.0",
    "sass": "^1.69.5",
    "tailwindcss": "^3.3.5",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "gsap": "^3.12.2"
  }
}
